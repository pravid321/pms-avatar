export interface IChildPolicy {

    charge: number,
    childPolicyId?: number,
    childPolicyName: string,
    editable?: boolean,
    maxAge: number,
    minAge: number,
    percentageOfBooking: string,
    policyID?: number

}